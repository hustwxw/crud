import styles from './common.less'

const rowClassName = `${styles['search-row']}`
const tableClassName = `${styles['search-row']} ${styles['table-row']}`
const pagerClassName = `${styles['page-row']}`

export default {
  rowClassName: rowClassName,
  tableClassName: tableClassName,
  pagerClassName: pagerClassName
}
